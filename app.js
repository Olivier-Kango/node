const express = require('express');

const app = express();
console.log(app);

// app.get((req, res) => {
//   res.status(200).send('Hello world !')
// })

// app.listen(3001, () => {
//   console.log('en attente de la requête !')
// })
